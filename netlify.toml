[build]
  command = "npm install && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"

# Netlify automatically serves .html files without extensions (pretty URLs)
# Fallback: serve 404 page for unknown routes
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
